
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'about',
};


export default async function Page() {
    return (
        <main>
            about
        </main>
    );
}